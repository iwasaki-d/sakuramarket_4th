%h1 商品一覧
.row
  - @commodities.each do |commodity|
    .col-xs-4
      .panel.panel-default
        .panel-header
          = link_to commodity.name, commodity
        .panel-body
          = image_tag 'ball.png', size: '200x200'
        .panel-footer
          %p 価格 ¥#{commodity.price}
